/**
 * The product name, in one place.
 *
 * Changing these three strings renames the system everywhere — wordmark,
 * browser tab, login screen and sidebar. Nothing else needs touching.
 */
export const BRAND: {
  name: string;
  nameAr: string;
  tagline: string;
  taglineAr: string;
  company: string;
  companyAr: string;
} = {
  /** The wordmark. Rendered in the display face, uppercase. */
  name: "JARVIS",
  nameAr: "جارفس",
  /** The small label beside it. */
  tagline: "Internal CRM",
  taglineAr: "النظام الداخلي",
  /** Used in the browser tab and the footer. */
  company: "Jarvis AI Agency",
  companyAr: "وكالة جارفس للذكاء الاصطناعي",
};
